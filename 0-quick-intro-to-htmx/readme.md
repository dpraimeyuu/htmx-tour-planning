### Exercise
Go to `./exercise` and explore using HTMX by adding a capability to `<button>` so that whenever its clicked then it interacts with a hypermedia endpoint to fetch HTML content and put it in `#container` element.

You can peek solution by using `./check-solution.sh 0-quick-intro-to-htmx` script from the root of the repo.